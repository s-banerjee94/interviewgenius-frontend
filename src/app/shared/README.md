# Shared Module

Contains reusable components, directives, and pipes that are shared across features.

## Structure
- `components/` - Reusable UI components
- `directives/` - Custom directives
- `pipes/` - Custom pipes
