# Core Module

Contains singleton services, guards, and interceptors that are used application-wide.

## Structure
- `services/` - Application-wide singleton services (e.g., auth, error handling, API)
- `guards/` - Route guards
- `interceptors/` - HTTP interceptors
